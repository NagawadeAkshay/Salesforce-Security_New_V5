@IsTest public with sharing class TestSampleObjectTriggerHelper {

    @TestSetup
    static void makeData(){
        NameTemplateConfig__c config = TestHelper.getNameTemplateConfig(true);
    }
    @IsTest static void testSampleObjectTriggerHelper(){
        SampleObjectTriggerHelper sample = new SampleObjectTriggerHelper();
        SampleObject11__c sampleObj = TestHelper.getSampleObject1(true);
        String test = 'Success';
        System.assertEquals('Success', test);
    }

    @IsTest static void testGetAutoGeneratedNumber(){
        NameTemplateTriggerHelper.getAutoGeneratedNumber('{AN-001}',0001);
        String test = 'Success';
        System.assertEquals('Success', test);
    }
}